{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Start backend db",
        "type": "shell",
        "command": "docker-compose -f .docker/docker-compose.local.yml --project-name njord-backend up -d db",
        "windows": {
          "command": "docker-compose -f .docker/docker-compose.local.yml --project-name njord-backend up -d db"
        },
        "runOptions": {
          "runOn": "folderOpen"
        },
        "presentation": {
          "reveal": "always",
          "panel": "new"
        }
      },
    ]
  }
  